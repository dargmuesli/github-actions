name: 'CI'

on:
  pull_request:
  push:

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - name: Git checkout
        uses: actions/checkout@dcd71f646680f2efd8db4afa5ad64fdcba30e748 # tag=v3.0.1
      - name: Lint yaml
        uses: ibiqlik/action-yamllint@v3
        with:
          config_data: |
            extends: default
            rules:
              colons:
                level: warning
              line-length:
                level: warning
    # uses: dargmuesli/github-actions/.github/workflows/lint-workflow-yaml.yml@0.6.1
    # secrets:
    #   GH_TOKEN: ${{ secrets.GH_TOKEN }}
  release-semantic:
    needs: lint
    name: Semantic Release
    uses: dargmuesli/github-actions/.github/workflows/release-semantic.yml@0.10.2
    secrets:
      GH_TOKEN: ${{ secrets.GH_TOKEN }}
